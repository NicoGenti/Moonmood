---
phase: 02-oracle-response
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [ORCL-01, ORCL-02, ORCL-03, ORCL-04, ORCL-05, ASTR-01, ASTR-02, ASTR-03]

must_haves:
  truths:
    - "User saves mood on / and is navigated to /oracle"
    - "Oracle card performs a 3D tarot flip animation"
    - "Remedy fades in after card flip completes"
    - "Different moon phases and mood scores produce visibly different cards over multiple test saves"
    - "Navigating directly to /oracle without a saved log shows Italian placeholder with home CTA"
    - "Oracle page survives a hard browser refresh (reads from IndexedDB)"
  artifacts:
    - path: "src/app/oracle/page.tsx"
      provides: "Verified oracle result page"
      contains: "getTodayLog"
    - path: "src/components/oracle/OracleCardDisplay.tsx"
      provides: "Verified 3D flip animation"
      contains: "rotateY"
  key_links:
    - from: "/ save button"
      to: "/oracle"
      via: "router.push('/oracle') in useDailySession.saveLog()"
      pattern: "router.push"
---

<objective>
Human verification of the complete Phase 2 oracle experience. Confirm all 5 success criteria from the roadmap are working correctly in the browser.

Purpose: Phase 2 delivers a visual/interactive oracle reveal — automated tests can verify the logic but cannot confirm the animation feels right, the Italian copy is correct, or the overall flow is smooth.
Output: User approval that Phase 2 is complete.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-oracle-response/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated checks before human review</name>
  <files></files>
  <action>
    Run the full automated validation suite:
    1. `pnpm test` — all tests must pass
    2. `pnpm typecheck` — zero type errors
    3. `pnpm build` — production build succeeds
    4. `pnpm dev` — start dev server (do NOT stop it — user needs it running for verification)
    
    Report the results of steps 1-3. Confirm the dev server is running on http://localhost:3000.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>All 3 automated checks pass. Dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 2 oracle experience</name>
  <what-built>
    Complete Phase 2 Oracle Response experience:
    - useDailySession.saveLog() now captures lunar phase, runs oracle selection, saves oracle IDs to IndexedDB, and navigates to /oracle
    - /oracle page reads today's log from IndexedDB on mount (stateless)
    - OracleCardDisplay shows a 3D Y-axis tarot flip (1.2s, deliberate pacing)
    - SuggestedRemedy fades in below the card after flip completes
    - Oracle page shows moon phase name in Italian (e.g., "Luna Piena")
    - Empty state when no log exists: Italian message + "Torna alla Home" CTA
    - 22 oracle cards + 22 remedies in Italian seed data covering full mood range and all 8 lunar phases
  </what-built>
  <how-to-verify>
    Visit http://localhost:3000 and test the following:

    **Test 1 — Full Oracle Flow (SC1 + SC2):**
    1. If today's log already exists, tap "Modifica" to enter edit mode
    2. Drag the LiquidSlider to a mood score (try 3 for a low mood)
    3. Tap "Salva" — you should be navigated to /oracle
    4. Watch the card flip from face-down to face-up (3D Y-axis rotation, ~1.2 seconds)
    5. Confirm the remedy text appears below the card after the flip
    ✓ Expected: Italian card name, Italian description, Italian remedy text

    **Test 2 — Oracle Page Reads from IndexedDB (SC5 + D-04):**
    1. While on /oracle, hard-refresh the page (Ctrl+Shift+R or Cmd+Shift+R)
    2. ✓ Expected: Oracle card and remedy are still visible (data from IndexedDB, not router state)

    **Test 3 — Empty State (D-05):**
    1. Open a new incognito window and visit http://localhost:3000/oracle directly
    2. ✓ Expected: Italian placeholder message + "Torna alla Home" button (no auto-redirect)

    **Test 4 — Different Cards for Different Moods (SC4):**
    1. Clear today's data: open DevTools → Application → IndexedDB → MoonmoodDB → dailyLogs → delete today's entry
    2. Save a mood of 1 → note which card appears on /oracle → go back to /
    3. Repeat with mood 9 → different card should appear
    ✓ Expected: Different scores produce visibly different card names and descriptions

    **Test 5 — Lunar Phase Shown (SC3 + ASTR-01):**
    1. On the /oracle page, check for the Italian lunar phase label (e.g., "Luna Piena", "Luna Crescente")
    2. ✓ Expected: Moon phase name visible, in Italian, no external API call (check Network tab — no astrology API calls)

    **Visual Quality Check:**
    - Card flip feels deliberate and mystical (not too fast)
    - Oracle page feels calm — card centered, remedy below, gradient background visible
    - All text is in Italian
    - Mobile layout looks correct (check with browser DevTools at 390px width)
  </how-to-verify>
  <resume-signal>Type "approvato" when all tests pass, or describe any issues found</resume-signal>
  <action>Follow the verification steps above. Test all 5 scenarios manually in the browser.</action>
  <verify>User approves by typing "approvato"</verify>
  <done>User has confirmed all 5 Phase 2 success criteria work correctly in the browser</done>
</task>

</tasks>

<verification>
Phase 2 is verified complete when the user types "approvato" confirming all 5 roadmap success criteria are met:
1. ✓ Submitting mood triggers oracle card reveal with animated transition
2. ✓ Remedy appears alongside oracle card
3. ✓ Lunar phase calculated client-side, shown on oracle page
4. ✓ Different scores/phases produce meaningfully different cards
5. ✓ Oracle content loads from bundled JSON — works offline
</verification>

<success_criteria>
User approves the oracle experience by typing "approvato".
Phase 2 Oracle Response is complete and ready for Phase 3 planning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-response/02-06-SUMMARY.md`

Then update:
- `.planning/STATE.md` — mark Phase 2 complete
- `.planning/ROADMAP.md` — mark Phase 2 plans complete, update progress table
</output>
